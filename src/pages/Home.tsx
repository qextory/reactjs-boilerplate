import { css } from '@emotion/react';

const Home = () => {
  return (
    <div
      css={css`
        display: flex;
        font-size: 24px;
      `}
    >
      React.js Bolierplate
    </div>
  );
};

export default Home;
